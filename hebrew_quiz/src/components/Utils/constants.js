// This forces the system to always use the first quiz in quiz sets that have multiple levels
// of quizzes. Change this in all places once multiple quizzes are supported.
export const FIRST_QUIZ = 0;
